---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - css/style.css
autonomous: true
requirements:
  - ABOUT-03
  - SERVICE-01
must_haves:
  truths:
    - "All About section h4 elements use CSS font-weight token instead of hardcoded 500"
    - "All Services section h2 and h4 elements use CSS font-weight token"
    - "Services accordion border-top uses CSS color token instead of hardcoded #333333"
    - "Dark theme consistency applied: About section background is dark"
  artifacts:
    - path: "css/style.css"
      provides: "Tokenized CSS rules for About and Services"
      lines: "281–413"
      exports: [
        ".about .content .box .inner .text h4",
        ".service .content .text h2",
        ".service .content .accordian-container .head h4",
        ".service .content .accordian-container .body"
      ]
    - path: "css/style.css"
      provides: "CSS custom properties (tokens) for font-weight and colors"
      lines: "1–50 (in :root)"
      exports: [
        "--fw-semibold",
        "--color-border"
      ]
  key_links:
    - from: ".about section styling"
      to: "CSS token --color-bg-secondary"
      via: "dark background override"
      pattern: "background-color: var\\(--color-bg-secondary\\)"
    - from: "all h4 and h2 headings"
      to: "CSS token --fw-semibold"
      via: "font-weight consistency"
      pattern: "font-weight: var\\(--fw-semibold\\)"
    - from: "accordion borders"
      to: "CSS token --color-border"
      via: "border color consistency"
      pattern: "border-top: 1px solid var\\(--color-border\\)"
---

<objective>
Tokenize hardcoded CSS values in About and Services sections to use centralized CSS custom properties. This refactoring improves maintainability, ensures design consistency, and prepares the site for future theme variations. About section dark background is verified; font weights and border colors are made token-based. No visual changes expected — tokens have already been defined in Phase 1 :root.

Purpose: Establish design system discipline across all sections; enable future theme scaling.
Output: CSS file with token-based rules for About and Services; no HTML changes
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-about-services/03-RESEARCH.md
@.planning/phases/03-about-services/03-CONTEXT.md
@.planning/phases/01-brand-foundation/01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 03-03: Tokenize CSS Font Weights and Colors in About & Services Sections</name>
  <files>css/style.css</files>
  <action>
Replace all hardcoded CSS font-weight values (hardcoded `500`) and color values in About and Services sections with CSS custom property tokens. This ensures design consistency and improves maintainability.

**Replacements (in css/style.css):**

1. **About section h4 font-weight (line ~306):**
   - Current: `.about .content .box .inner .text h4 { ... font-weight: 500; ... }`
   - Replace with: `font-weight: var(--fw-semibold);`
   - Rationale: Per D-07, font-weight `500` is a design token (`--fw-semibold` defined in Phase 1 :root)

2. **Services section h2 font-weight (line ~332):**
   - Current: `.service .content .text h2 { ... font-weight: 500; ... }`
   - Replace with: `font-weight: var(--fw-semibold);`
   - Rationale: Consistency with About and other headings

3. **Services section accordion item h4 font-weight (line ~391):**
   - Current: `.service .content .accordian-container .head h4 { ... font-weight: 500; ... }`
   - Replace with: `font-weight: var(--fw-semibold);`
   - Rationale: All h4 elements should use the same font-weight token

4. **Services accordion border color (line ~404):**
   - Current: `.service .content .accordian-container .body { ... border-top: 1px solid #333333; ... }`
   - Replace with: `border-top: 1px solid var(--color-border);`
   - Rationale: Per D-07, hardcoded border color should use token (--color-border defined in Phase 1)

**Verification points:**
- Phase 1 :root block contains `--fw-semibold` (should be ~500 or similar weight)
- Phase 1 :root block contains `--color-border` (should be ~#333333 or similar dark gray)
- No other hardcoded `500` font-weight values in About/Services sections after replacement
- No other hardcoded `#333333` color values in About/Services sections after replacement
- No visual change expected (token values match original hardcoded values)

**Implementation approach:**
1. Open `css/style.css`
2. Locate each rule mentioned above (search for `.about .content .box .inner .text h4`, `.service .content .text h2`, `.service .content .accordian-container .head h4`, `.service .content .accordian-container .body`)
3. Replace `font-weight: 500;` with `font-weight: var(--fw-semibold);` (do this 3 times)
4. Replace `border-top: 1px solid #333333;` with `border-top: 1px solid var(--color-border);` (do this 1 time)
5. Verify all replacements are in place
6. Commit with message referencing D-07

Per D-01, About section background is already dark (token applied in Phase 1); no change needed there.
  </action>
  <verify>
    <automated>grep "font-weight: 500" css/style.css | grep -c "about\|service" && echo "FAIL: Hardcoded font-weight 500 still present" || echo "PASS: No hardcoded font-weight 500"</automated>
    <automated>grep "border-top: 1px solid #333333" css/style.css && echo "FAIL: Hardcoded border color still present" || echo "PASS: Border color tokenized"</automated>
    <automated>grep -c "var(--fw-semibold)" css/style.css && echo "Font-weight token found" || echo "FAIL: Font-weight token not applied"</automated>
    <automated>grep -c "var(--color-border)" css/style.css && echo "Border color token found" || echo "FAIL: Border color token not applied"</automated>
  </verify>
  <done>
    ✅ About section h4 font-weight changed from hardcoded `500` to `var(--fw-semibold)`
    ✅ Services section h2 font-weight changed from hardcoded `500` to `var(--fw-semibold)`
    ✅ Services section accordion h4 font-weight changed from hardcoded `500` to `var(--fw-semibold)`
    ✅ Services accordion border-top changed from hardcoded `#333333` to `var(--color-border)`
    ✅ No other hardcoded font-weight or border-color values remain in About/Services sections
    ✅ CSS custom property tokens already defined in Phase 1 :root (--fw-semibold, --color-border)
    ✅ No visual change expected (token values match original hardcoded values)
    ✅ Design system consistency achieved across all sections
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Severity |
|----------|-------------|----------|
| CSS token definition→usage | Token must be defined in :root before use | Low |
| Token value accuracy | Token value must match original hardcoded value | Medium |
| Selector specificity | CSS selector must not change; only property values change | Low |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | CSS rule integrity | Mitigate | Verify each replacement preserves selector and other properties; grep for hardcoded values post-replacement |
| T-03-12 | Information Disclosure | Token value mismatch | Mitigate | Inspect Phase 1 :root to confirm token values match original hardcoded values; visual test in browser |
| T-03-13 | Denial of Service | CSS parse error | Mitigate | Validate CSS syntax after changes; run CSS linter if available |
| T-03-14 | Elevation of Privilege | None applicable | — | N/A |
| T-03-15 | Repudiation | Design decision traceability | Accept | Commit message references D-07; git history shows tokenization |
| T-03-16 | Information Disclosure | Other sections affected | Mitigate | Grep confirms tokenization changes only About/Services sections |

</threat_model>

<verification>
## Phase 3–03 Verification Checklist

- [ ] Phase 1 :root contains `--fw-semibold` CSS custom property
- [ ] Phase 1 :root contains `--color-border` CSS custom property
- [ ] `.about .content .box .inner .text h4` has `font-weight: var(--fw-semibold);`
- [ ] `.service .content .text h2` has `font-weight: var(--fw-semibold);`
- [ ] `.service .content .accordian-container .head h4` has `font-weight: var(--fw-semibold);`
- [ ] `.service .content .accordian-container .body` has `border-top: 1px solid var(--color-border);`
- [ ] `grep "font-weight: 500" css/style.css` returns 0 matches (or only in non-About/Services sections)
- [ ] `grep "#333333" css/style.css` returns 0 matches (or only in non-About/Services sections)
- [ ] CSS file parses without syntax errors (validate with browser DevTools or CSS linter)
- [ ] Visual inspection: About h4 headings appear the same weight (no visual change)
- [ ] Visual inspection: Services h2 heading appears the same weight (no visual change)
- [ ] Visual inspection: Accordion h4 headings appear the same weight (no visual change)
- [ ] Visual inspection: Accordion border color unchanged (no visual change)
- [ ] Visual inspection: Dark background still applied to About section
- [ ] Visual inspection: Dark styling still applied to Services section
- [ ] No DOM changes — HTML remains identical

</verification>

<success_criteria>
✅ **CSS Tokenization Complete:** All hardcoded font-weight and border-color values in About/Services replaced with CSS custom property tokens
✅ **Token Consistency:** About h4, Services h2, Services h4 all use `--fw-semibold`
✅ **Border Token Applied:** Accordion border uses `--color-border`
✅ **No Visual Change:** Browser rendering identical (tokens match original hardcoded values)
✅ **Design System Compliance:** About and Services sections now follow Phase 1 design token system
✅ **Maintainability Improved:** Future theme updates can modify tokens in one place (Phase 1 :root)
✅ **No Regressions:** No other CSS sections affected; only About and Services changed
</success_criteria>

<output>
Create `.planning/phases/03-about-services/03-03-SUMMARY.md` after task completion
</output>
