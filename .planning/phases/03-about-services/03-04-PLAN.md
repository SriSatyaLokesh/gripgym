---
phase: "03"
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - index.html
autonomous: true
requirements:
  - SERVICE-04
must_haves:
  truths:
    - "\"Start Now\" button href attribute is set to a valid section anchor"
    - "Button click scrolls to correct section without page reload"
    - "Button maintains hover state styling"
    - "Animation and interaction behavior preserved"
  artifacts:
    - path: "index.html"
      provides: "Services section \"Start Now\" button with href anchor"
      lines: "133–135"
      exports: ["<a href=\"#TARGET_SECTION\" class=\"btn\">Start Now</a>"]
    - path: "index.html"
      provides: "Target section anchor exists in page"
      lines: "variable (depends on anchor choice)"
      exports: ["id=\"TARGET_SECTION\""]
  key_links:
    - from: ".service .content .text .btn"
      to: "CSS hover state"
      via: "CSS selector"
      pattern: "<a href=\"#classes\" class=\"btn\">Start Now</a>"
    - from: "button href anchor"
      to: "target section id"
      via: "HTML anchor linkage"
      pattern: "<section id=\"classes\">"
---

<objective>
Set the "Start Now" button href anchor to connect Services section to the next logical step in the visitor journey. The button is currently empty; this task assigns the anchor based on user preference or default choice (#classes as per D-06). No HTML restructuring needed — only the href attribute value changes.

Purpose: Enable clear call-to-action flow: Services (what GripGym offers) → Next Section (when classes run / how to engage)
Output: index.html with "Start Now" button href set to anchor destination
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-about-services/03-RESEARCH.md
@.planning/phases/03-about-services/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 03-04: Set \"Start Now\" Button Anchor and Verify Navigation Flow</name>
  <files>index.html</files>
  <action>
Set the "Start Now" button href attribute (line 133–135 in index.html) to connect Services section to the next section in the visitor journey.

**Current state:**
```html
<a href="" class="btn">Start Now</a>
```

**Update to one of three options (user to confirm which):**

**Option A (DEFAULT per D-06):** `#classes` — Natural progression (About → Services → Classes)
```html
<a href="#classes" class="btn">Start Now</a>
```
- Rationale: Logical flow — visitor learns what services exist, then sees when/where classes happen
- Target section: `<section class="classes" id="classes">` (exists at line ~188 in index.html)

**Option B:** `#price` — Immediate pricing/upsell
```html
<a href="#price" class="btn">Start Now</a>
```
- Rationale: Visitor ready to commit; show pricing tiers and membership options
- Target section: `<section class="price" id="price">` (if exists)

**Option C:** `#contact` — Direct contact path
```html
<a href="#contact" class="btn">Start Now</a>
```
- Rationale: Visitor wants immediate inquiry/consultation
- Target section: `<section class="contact" id="contact">` (if exists)

**Implementation:**
1. Open `index.html`
2. Locate the "Start Now" button in Services section (search for `class="btn">Start Now</a>`)
3. Replace empty href `href=""` with one of the three options above
4. Verify that the target section (e.g., `<section id="classes">`) exists in the page
5. Test in browser: click button → page smoothly scrolls to target section

**Per D-06:** Default anchor is `#classes` unless user explicitly specifies otherwise.

**HTML changes minimal:**
- Only the `href` attribute value changes
- Button text remains "Start Now"
- Button class remains `btn`
- All button styling and hover states preserved
- No animation or interaction changes

**Verification approach:**
- Grep confirms href is set to one of the three valid anchors
- Grep confirms target section exists with matching id
- Browser test: click button → smooth scroll to target section
  </action>
  <verify>
    <automated>grep 'href="#classes"' index.html && echo "PASS: Button href set to #classes" || echo "FAIL or NOT SET: Check button href"</automated>
    <automated>grep 'id="classes"' index.html && echo "PASS: Classes section anchor exists" || echo "FAIL: Classes section not found"</automated>
    <automated>grep 'class="btn">Start Now</a>' index.html && echo "PASS: Button text and class intact" || echo "FAIL: Button structure changed"</automated>
  </verify>
  <done>
    ✅ "Start Now" button href attribute set to valid section anchor (#classes, #price, or #contact)
    ✅ Target section with matching id exists in index.html
    ✅ Button text remains "Start Now" (unchanged)
    ✅ Button class remains "btn" (unchanged)
    ✅ Button styling and hover states preserved (CSS intact)
    ✅ Smooth scroll behavior tested in browser (no page reload on click)
    ✅ Anchor destination visible and content loads without errors
    ✅ No other button elements affected (only Services "Start Now" button changed)
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Where should the "Start Now" button in Services section link to?</decision>
  <context>
The "Start Now" button currently has an empty href. Three logical options:

1. **#classes** (DEFAULT) — Next section shows class schedule, times, trainer info
   - Natural progression: "What we offer (Services) → When it happens (Classes)"
   - Best for visitor ready to explore offerings

2. **#price** — Next section shows membership tiers and pricing
   - Immediate upsell: "What we offer → How much it costs"
   - Best for price-sensitive decision-making

3. **#contact** — Jump to contact form
   - Direct inquiry: "What we offer → Let's talk"
   - Best for interested visitors wanting to ask questions

Based on D-06, default is **#classes** unless you specify otherwise.
  </context>
  <options>
    <option id="option-classes">
      <name>Link to Classes section (#classes)</name>
      <pros>
        - Natural user flow progression
        - Visitor sees when/where classes run
        - Aligns with typical gym site structure
      </pros>
      <cons>
        - Slightly longer journey before pricing/contact
      </cons>
    </option>
    <option id="option-price">
      <name>Link to Pricing section (#price)</name>
      <pros>
        - Immediate transparency on cost
        - Captures price-sensitive visitors
      </pros>
      <cons>
        - Skips class schedule context
        - May not align with visitor readiness
      </cons>
    </option>
    <option id="option-contact">
      <name>Link to Contact section (#contact)</name>
      <pros>
        - Direct engagement with inquiry form
        - Captures interested visitors wanting to ask questions
      </pros>
      <cons>
        - Skips pricing and schedule context
      </cons>
    </option>
  </options>
  <resume-signal>Select: option-classes (default), option-price, or option-contact</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Severity |
|----------|-------------|----------|
| Button href→target anchor | Anchor href must match an existing section id | High |
| Click behavior | Button click must smoothly scroll without JavaScript errors | Medium |
| Button styling | Hover/active states must remain functional | Low |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering | href attribute integrity | Mitigate | Verify href is set to valid anchor; grep for "href=\"#" and cross-check with existing section ids |
| T-03-18 | Information Disclosure | Button click redirects to correct section | Mitigate | Test button click in browser; verify smooth scroll to target section |
| T-03-19 | Denial of Service | Broken anchor link | Mitigate | Confirm target section exists before finalizing href value |
| T-03-20 | Elevation of Privilege | None applicable | — | N/A |
| T-03-21 | Repudiation | CTA decision tracking | Accept | User confirms anchor choice via checkpoint; decision documented in plan |
| T-03-22 | Information Disclosure | CSS hover state functionality | Accept | Button styling unchanged; no risk to hover behavior |

</threat_model>

<verification>
## Phase 3–04 Verification Checklist

- [ ] User confirms which anchor destination for "Start Now" button (checkpoint completed)
- [ ] "Start Now" button href attribute is set to chosen anchor (#classes, #price, or #contact)
- [ ] Target section exists in index.html with matching id attribute
- [ ] `grep 'href="#classes"' index.html` OR `href="#price"` OR `href="#contact"` returns 1 match
- [ ] Button text "Start Now" preserved
- [ ] Button class="btn" preserved
- [ ] No other button styling changed
- [ ] Browser test: Click "Start Now" button → page smoothly scrolls to target section
- [ ] No JavaScript errors in browser console on button click
- [ ] Target section content is visible and rendered correctly after scroll
- [ ] Button hover state still works (CSS .btn:hover preserved)
- [ ] No impact on other links or buttons in page
- [ ] Responsive behavior: Button clickable and functional on 375px, 550px, 767px viewports

</verification>

<success_criteria>
✅ **Decision Made:** User selected target anchor for "Start Now" button (via checkpoint)
✅ **Anchor Set:** Button href attribute updated to chosen destination
✅ **Target Verified:** Target section exists in page with matching id
✅ **Button Functional:** Click scrolls to target section without errors
✅ **Styling Preserved:** Button styling and hover states intact
✅ **Flow Complete:** Visitor journey connected: Services (what) → Target (when/price/contact)
✅ **No Regressions:** No other page elements affected
</success_criteria>

<content_handoff>
## User Decision Required for Task 03-04

User must confirm which anchor destination for the "Start Now" button before task execution.

**Three options:**

1. **#classes** (RECOMMENDED) — Link to Classes section
   - Shows class schedule, times, and trainer info
   - Natural progression: learn what services → see when they run

2. **#price** — Link to Pricing section
   - Shows membership tiers and pricing
   - Immediate cost transparency

3. **#contact** — Link to Contact section
   - Shows contact form and information
   - Direct visitor inquiry path

**Default (per D-06):** #classes

**User input format:**
Select one of: `option-classes`, `option-price`, or `option-contact`

**File Impact:**
- `index.html`: Line 133 (button href attribute) receives chosen anchor value
- `css/style.css`: No changes

**Commit Message Template:**
```
feat(03-04): set start now button anchor to {selected_option}

- Button href set to #{anchor}
- Links to {target_section} section
- Preserves button styling and interaction
```

</content_handoff>

<output>
Create `.planning/phases/03-about-services/03-04-SUMMARY.md` after task completion
</output>
