---
phase: 05-pricing-contact
plan: 3
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - css/style.css
autonomous: true
requirements:
  - PRICING-03
  - CONTACT-02
user_setup: []
must_haves:
  truths:
    - "Pricing and Contact sections have dark theme backgrounds matching phases 1-4"
    - "Pricing cards display in responsive grid (4 cols desktop, 2 cols tablet, 1 col mobile)"
    - "Contact section has 2-column layout (info left, form right) on desktop, single column on mobile"
    - "All form inputs and buttons are styled consistently with existing design"
    - "Hover states visible on pricing cards and buttons"
    - "Typography uses established CSS tokens (font-family, font-weight, color)"
  artifacts:
    - path: "css/style.css"
      provides: "Styling for .pricing and .contact sections with dark theme and responsive layout"
      exports: [".pricing", ".pricing-card", ".pricing-grid", ".contact", ".contact-form", "@media (max-width:"]
      min_lines: 100
  key_links:
    - from: "css/style.css .pricing"
      to: "css/style.css design tokens"
      via: "background-color: var(--color-bg-secondary), color: var(--color-text)"
      pattern: "var\\(--color-bg-(secondary|primary)\\)"
    - from: "css/style.css .pricing-grid"
      to: "css/style.css @media queries"
      via: "grid-template-columns responsive at 991px, 767px, 550px breakpoints"
      pattern: "@media \\(max-width.*grid-template-columns"
    - from: "html form inputs"
      to: "css/style.css input/textarea styling"
      via: "background-color, border, color tokens applied"
      pattern: "input,.*textarea.*{.*background-color:"
---

<objective>
Style Pricing and Contact sections with dark theme consistency and responsive design.

**Purpose:** 
- Apply dark theme backgrounds and typography to match phases 1-4 (D-06)
- Implement responsive grid layout for pricing cards (4→2→1 columns per D-07)
- Style contact form with consistent input/button styling
- Ensure all text, borders, and accents use CSS tokens (no hardcoded hex values)
- Verify responsive design at all breakpoints (991px, 767px, 550px, 479px)

**Output:**
- CSS rules for `.pricing` section with responsive grid layout
- CSS rules for `.contact` section with 2-column desktop/1-column mobile layout
- All dark theme colors applied via `--color-bg-secondary`, `--color-bg-primary`, `--color-text`, `--color-primary`
- Form inputs and buttons styled consistently
- All breakpoints tested and responsive
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-classes-schedule/04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 05-04: Add CSS styling for Pricing and Contact sections (dark theme, responsive)</name>
  <files>css/style.css</files>
  <action>
**CSS Changes (css/style.css):**

Add the following CSS rules at the end of the file (before closing `}`):

```css
/* ============================================================================
   PRICING SECTION
   ============================================================================ */

.pricing {
  padding: 80px 0;
  background-color: var(--color-bg-secondary);
}

.pricing h1 {
  font-size: 2.5rem;
  font-weight: var(--fw-bold);
  color: var(--color-text);
  text-align: center;
  margin-bottom: 20px;
}

.pricing .intro-text {
  font-size: 1.1rem;
  color: var(--color-text-secondary);
  text-align: center;
  margin-bottom: 60px;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

.pricing-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 30px;
  margin-bottom: 40px;
}

.pricing-card {
  background-color: var(--color-bg-primary);
  border: 2px solid var(--color-border);
  border-radius: 8px;
  padding: 30px;
  display: flex;
  flex-direction: column;
  transition: all 0.3s ease;
}

.pricing-card:hover {
  border-color: var(--color-primary);
  transform: translateY(-5px);
}

.pricing-card.featured {
  border-color: var(--color-primary);
  box-shadow: 0 0 20px rgba(232, 25, 44, 0.3);
}

.pricing-card h3 {
  font-size: 1.5rem;
  font-weight: var(--fw-bold);
  color: var(--color-text);
  margin-bottom: 20px;
  text-align: center;
}

.pricing-card .price {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 20px;
  padding: 20px 0;
  border-bottom: 1px solid var(--color-border);
}

.pricing-card .price-number {
  font-size: 2.5rem;
  font-weight: var(--fw-bold);
  color: var(--color-primary);
}

.pricing-card .price-duration {
  font-size: 0.9rem;
  color: var(--color-text-secondary);
  margin-top: 5px;
}

.pricing-card .discount-label {
  text-align: center;
  color: var(--color-primary);
  font-weight: var(--fw-semibold);
  font-size: 0.9rem;
  margin-bottom: 15px;
}

.pricing-card .features {
  list-style: none;
  padding: 0;
  margin: 0 0 30px;
  flex-grow: 1;
}

.pricing-card .features li {
  padding: 10px 0;
  color: var(--color-text-secondary);
  font-size: 0.95rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.pricing-card .features li:before {
  content: "✓ ";
  color: var(--color-primary);
  font-weight: var(--fw-bold);
  margin-right: 10px;
}

.pricing-card .cta-container {
  display: flex;
  justify-content: center;
}

.pricing-card .cta-button {
  background-color: var(--color-primary);
  color: white;
  border: none;
  padding: 12px 30px;
  border-radius: 5px;
  font-weight: var(--fw-semibold);
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.pricing-card .cta-button:hover {
  background-color: #c0182a;
  transform: scale(1.05);
}

/* ============================================================================
   CONTACT SECTION
   ============================================================================ */

.contact {
  padding: 80px 0;
  background-color: var(--color-bg-primary);
}

.contact h1 {
  font-size: 2.5rem;
  font-weight: var(--fw-bold);
  color: var(--color-text);
  text-align: center;
  margin-bottom: 60px;
}

.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
}

.contact-info {
  display: flex;
  flex-direction: column;
  gap: 40px;
}

.contact-item h4 {
  font-size: 1.2rem;
  font-weight: var(--fw-bold);
  color: var(--color-primary);
  margin-bottom: 10px;
}

.contact-item p,
.contact-item a {
  font-size: 1rem;
  color: var(--color-text-secondary);
  line-height: 1.6;
}

.contact-item a {
  color: var(--color-primary);
  text-decoration: none;
  transition: color 0.3s ease;
}

.contact-item a:hover {
  text-decoration: underline;
}

.contact-form {
  display: flex;
  flex-direction: column;
}

.contact-form form {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.contact-form input,
.contact-form textarea {
  background-color: var(--color-bg-secondary);
  border: 1px solid var(--color-border);
  color: var(--color-text);
  padding: 15px;
  border-radius: 5px;
  font-family: inherit;
  font-size: 1rem;
  transition: border-color 0.3s ease;
}

.contact-form input::placeholder,
.contact-form textarea::placeholder {
  color: var(--color-text-secondary);
}

.contact-form input:focus,
.contact-form textarea:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 10px rgba(232, 25, 44, 0.2);
}

.contact-form button {
  background-color: var(--color-primary);
  color: white;
  border: none;
  padding: 15px 30px;
  border-radius: 5px;
  font-weight: var(--fw-semibold);
  font-size: 1rem;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.contact-form button:hover {
  background-color: #c0182a;
}

.form-message {
  margin-top: 20px;
  padding: 15px;
  border-radius: 5px;
  text-align: center;
  min-height: 20px;
  transition: opacity 0.3s ease;
}

.form-message.success {
  background-color: rgba(76, 175, 80, 0.2);
  color: #4caf50;
  border: 1px solid #4caf50;
}

.form-message.error {
  background-color: rgba(244, 67, 54, 0.2);
  color: #f44336;
  border: 1px solid #f44336;
}

/* ============================================================================
   RESPONSIVE BREAKPOINTS
   ============================================================================ */

/* Tablet (991px and below) */
@media (max-width: 991px) {
  .pricing-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 25px;
  }
  
  .contact-grid {
    grid-template-columns: 1fr;
    gap: 40px;
  }
}

/* Mobile (767px and below) */
@media (max-width: 767px) {
  .pricing {
    padding: 60px 0;
  }
  
  .pricing h1 {
    font-size: 2rem;
  }
  
  .pricing-grid {
    grid-template-columns: 1fr;
    gap: 20px;
  }
  
  .pricing-card {
    padding: 25px;
  }
  
  .pricing-card h3 {
    font-size: 1.3rem;
  }
  
  .pricing-card .price-number {
    font-size: 2rem;
  }
  
  .contact {
    padding: 60px 0;
  }
  
  .contact h1 {
    font-size: 2rem;
    margin-bottom: 40px;
  }
  
  .contact-info {
    gap: 30px;
  }
  
  .contact-form input,
  .contact-form textarea {
    padding: 12px;
    font-size: 16px;
  }
  
  .contact-form button {
    padding: 12px 20px;
  }
}

/* Small Mobile (550px and below) */
@media (max-width: 550px) {
  .pricing h1 {
    font-size: 1.7rem;
    margin-bottom: 15px;
  }
  
  .pricing .intro-text {
    font-size: 1rem;
    margin-bottom: 40px;
  }
  
  .pricing-card {
    padding: 20px;
  }
  
  .pricing-card h3 {
    font-size: 1.2rem;
  }
  
  .pricing-card .price-number {
    font-size: 1.8rem;
  }
  
  .pricing-card .features li {
    padding: 8px 0;
    font-size: 0.9rem;
  }
  
  .contact h1 {
    font-size: 1.7rem;
  }
  
  .contact-item h4 {
    font-size: 1rem;
  }
  
  .contact-item p,
  .contact-item a {
    font-size: 0.95rem;
  }
}

/* Extra Small Mobile (479px and below) */
@media (max-width: 479px) {
  .pricing {
    padding: 40px 0;
  }
  
  .pricing h1 {
    font-size: 1.5rem;
  }
  
  .pricing-card {
    padding: 15px;
  }
  
  .pricing-card h3 {
    font-size: 1.1rem;
  }
  
  .pricing-card .price-number {
    font-size: 1.6rem;
  }
  
  .pricing-card .cta-button {
    padding: 10px 20px;
    font-size: 0.95rem;
  }
  
  .contact {
    padding: 40px 0;
  }
  
  .contact h1 {
    font-size: 1.5rem;
    margin-bottom: 30px;
  }
}
```

**Per D-06, D-07:** All colors use CSS tokens (no hardcoded hex values except for dark hover states). Responsive breakpoints match phases 1-4 (991px, 767px, 550px, 479px).
  </action>
  <verify>
    <automated>
Verify CSS applied correctly:
- Check file exists: `grep -c ".pricing {" css/style.css` (should be >= 1)
- Check dark background: `grep "background-color: var(--color-bg-secondary)" css/style.css` (should exist)
- Check pricing grid: `grep "grid-template-columns: repeat(4" css/style.css` (should exist)
- Check contact grid: `grep "grid-template-columns: 1fr 1fr" css/style.css` (should exist)
- Check responsive media queries: `grep -c "@media" css/style.css` (should be >= 4)
    </automated>
  </verify>
  <done>
✅ CSS styling added for Pricing and Contact sections
✅ Dark theme backgrounds applied (var(--color-bg-secondary), var(--color-bg-primary))
✅ Pricing grid responsive (4→2→1 columns at breakpoints)
✅ Contact layout responsive (2-column desktop → 1-column mobile)
✅ Form inputs and buttons styled consistently
✅ Hover states implemented on cards and buttons
✅ All colors use CSS tokens (no hardcoded hex except theme-specific overrides)
✅ Responsive breakpoints implemented (991px, 767px, 550px, 479px)
  </done>
</task>

<task type="auto">
  <name>Task 05-05: Phase 5 verification and final checks</name>
  <files>index.html, css/style.css, js/content-loader.js, data/content.json</files>
  <action>
**Verification Checklist (all items must PASS):**

1. **JSON Content Validation**
   - ✓ `data/content.json` loads successfully (Network tab shows 200 OK)
   - ✓ All 4 pricing tiers present with real prices (999, 1999, 3499, 35999)
   - ✓ All pricing features match specification (no Lorem Ipsum)
   - ✓ Contact info complete: phone, email, address, hours
   - ✓ JSON syntax is valid (no parse errors in console)

2. **Pricing Section**
   - ✓ 4 pricing cards display correctly
   - ✓ Each card shows: name, price (₹ symbol), duration, feature list
   - ✓ Prices match JSON (999, 1999, 3499, 35999)
   - ✓ Features rendered from JSON (check DevTools — no hardcoded HTML values)
   - ✓ Elite tier highlighted with "featured" styling (border/shadow)
   - ✓ Annual tier shows "Save ₹6,000/year" discount label
   - ✓ Select Plan buttons clickable (scroll to contact section)
   - ✓ Dark background matches hero/about/services sections

3. **Contact Section**
   - ✓ Contact info displays: address, phone, email, weekday/weekend hours
   - ✓ Phone and email are clickable links (tel:, mailto:)
   - ✓ Contact form has three input fields: name, email, message
   - ✓ All form fields are required (HTML5 validation)
   - ✓ Form submission shows success message
   - ✓ Form fields clear after submission
   - ✓ Dark background applied

4. **Styling & Theme Consistency**
   - ✓ Both sections use dark theme (backgrounds, text color)
   - ✓ No white backgrounds; matches phase 1-4 color palette
   - ✓ Accent color (₹ symbol, buttons, highlights) is consistent red (#e8192c)
   - ✓ Typography matches existing sections (font family, weights)
   - ✓ Hover states visible on cards and buttons

5. **Responsive Design**
   - ✓ Desktop (1200px+): 4-column pricing grid, 2-column contact layout
   - ✓ Tablet (991px): 2-column pricing grid, single-column contact
   - ✓ Mobile (767px): 1-column pricing grid, single-column contact
   - ✓ Small mobile (550px): All layouts readable, text scaled appropriately
   - ✓ Test at 375px: Pricing cards and contact form fully readable

6. **XSS Safety & Security**
   - ✓ Inspect pricing cards in DevTools: No hardcoded content (all from JSON)
   - ✓ Inspect contact info: All from JSON (phone, email, address, hours)
   - ✓ No innerHTML used for content (verify with grep: `grep -i "innerHTML.*contact\|innerHTML.*pricing" js/content-loader.js` should be empty for content)
   - ✓ Form validation present; no injection vulnerabilities

7. **No Console Errors**
   - ✓ Open DevTools → Console
   - ✓ No 404 errors for `data/content.json`, `js/content-loader.js`
   - ✓ No JSON parse errors
   - ✓ No form submission errors

8. **Content Accuracy (No Lorem Ipsum)**
   - ✓ All pricing tier names are real (Basic, Premium, Elite, Annual Elite)
   - ✓ All features are real gym benefits (no Lorem Ipsum)
   - ✓ All contact details are real GripGym info
   - ✓ Pricing intro is real ("Flexible membership plans...")

9. **Animations (Optional — WOW.js)**
   - ✓ If WOW.js animations added to sections, verify they trigger on scroll
   - (Optional: per D-08, animations are optional for this phase)

10. **Requirement Coverage**
    - ✓ PRICING-01: Pricing section displays 3+ membership tiers ✓ (4 tiers)
    - ✓ PRICING-02: Each tier shows price, duration, benefits ✓
    - ✓ PRICING-03: Pricing section matches dark theme ✓
    - ✓ CONTACT-01: Contact form with name, email, message fields ✓
    - ✓ CONTACT-02: Contact section displays address, phone, email ✓

**Manual Testing Steps:**

1. Open `index.html` in browser
2. Scroll to Pricing section → verify 4 cards display
3. Inspect first pricing card → verify all content loaded from JSON (right-click → Inspect → check `<li>` tags)
4. Click "Select Plan" button on any card → verify smooth scroll to contact section
5. Scroll to Contact section → verify address, phone, email, hours display
6. Click phone number → verify tel: link opens dialer (or system default)
7. Click email → verify mailto: link opens email client
8. Fill contact form (name, email, message) → click Send → verify success message appears
9. Resize browser to 375px width → verify all content readable (use DevTools device toolbar)
10. Open Network tab → reload page → verify `data/content.json` loads 200 OK

**Exit Criteria:**
- All 10 verification items PASS
- No console errors
- All 5 requirements met (PRICING-01, 02, 03, CONTACT-01, 02)
- Responsive design verified at 375px, 767px, 991px
- JSON content is source of truth (no hardcoded HTML values)
  </action>
  <verify>
    <automated>
Final verification commands:
- Content load: `curl -s http://localhost:3000/data/content.json | jq .pricing.tiers | head -20`
- Pricing count: `grep -c "pricing-card" index.html` (should be 0 — all rendered from JS, not HTML)
- Contact form: `grep -c "contact-form" index.html` (should be 1)
- CSS tokens: `grep -c "var(--color" css/style.css` (should be >= 10)
- No hardcoded prices in HTML: `grep -E "999|1999|3499|35999" index.html` (should be empty)
- No hardcoded contact in HTML: `grep "9876543210\|gripgym.com\|Old Market" index.html` (should be empty)
    </automated>
  </verify>
  <done>
✅ All 10 verification items passed
✅ PRICING-01: 4 tiers display with features
✅ PRICING-02: Price, duration, benefits visible
✅ PRICING-03: Dark theme applied
✅ CONTACT-01: Form with name/email/message fields
✅ CONTACT-02: Address, phone, email, hours display
✅ XSS-safe rendering (text nodes only, no innerHTML for content)
✅ JSON is single source of truth (no hardcoded prices/contact in HTML)
✅ Responsive design verified at 375px, 767px, 991px, desktop
✅ No console errors
✅ Form submission functional with success message
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → User device | Form data collected locally (no backend yet) |
| Static files → Browser | CSS and JS from source control (trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | CSS styling | accept | CSS files are source-controlled; modifications tracked by git |
| T-05-09 | Denial of Service | CSS file size | accept | CSS file size is minimal; no external CDN dependencies |
| T-05-10 | Information Disclosure | Contact form data | accept | Form data stored locally (no backend); user controls submission |

</threat_model>

<verification>
1. All CSS applied and responsive at breakpoints
2. Pricing and Contact sections styled with dark theme
3. Form inputs and buttons functional
4. All requirements met (PRICING-01, 02, 03, CONTACT-01, 02)
5. No console errors
6. Responsive design verified
</verification>

<success_criteria>
- [ ] Pricing section styled with dark background and responsive grid
- [ ] Contact section styled with dark background and responsive layout
- [ ] All form inputs and buttons styled consistently
- [ ] Responsive design verified at 375px, 767px, 991px
- [ ] All 5 requirements met (PRICING-01, 02, 03, CONTACT-01, 02)
- [ ] No console errors
- [ ] Dark theme matches phases 1-4 color palette
</success_criteria>

<output>
Create `.planning/phases/05-pricing-contact/05-03-SUMMARY.md` when complete
</output>
