---
phase: 05-pricing-contact
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - data/content.json
autonomous: true
requirements:
  - PRICING-01
  - PRICING-02
  - CONTACT-01
  - CONTACT-02
user_setup: []
must_haves:
  truths:
    - "Pricing & Contact sections will read all content from centralized data/content.json"
    - "JSON file contains all pricing tiers (4) with names, prices, durations, features"
    - "JSON file contains all contact info (phone, email, address, hours)"
    - "Future phases can extend this JSON to include phases 1-4 content"
  artifacts:
    - path: "data/content.json"
      provides: "Centralized content source for pricing and contact sections"
      exports: ["metadata", "pricing", "contact"]
      min_lines: 80
  key_links:
    - from: "data/content.json"
      to: "js/content-loader.js (Wave 2)"
      via: "fetch('data/content.json') → parse → render pricing tiers and contact info"
      pattern: "JSON.parse()"
    - from: "pricing.tiers array"
      to: "Pricing HTML cards"
      via: "4 tier objects with id, name, price, duration, features, discount_label"
      pattern: "\\\"id\\\":\\\"(basic|premium|elite|annual)\\\""
    - from: "contact object"
      to: "Contact HTML section"
      via: "phone, email, address, hours, social_media"
      pattern: "\\\"phone\\\":\\\"\\+91"
---

<objective>
Create centralized JSON content architecture for pricing and contact data.

**Purpose:** Establish single source of truth for all pricing tiers and contact information. This JSON file will be fetched and rendered by Waves 2–3 in Pricing and Contact sections. The structure is designed to be extended in Phase 6 to include content from Phases 1–4, enabling future content refactoring without code changes.

**Output:**
- `data/content.json` with metadata, pricing tiers (4), and contact information
- Valid JSON syntax (parseable by `JSON.parse()`)
- All GripGym-specific data (prices, contact details, feature lists)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 05-01: Create data/content.json with pricing tiers and contact information</name>
  <files>data/content.json</files>
  <action>
**Create new directory and file:**
1. Create `data/` directory in project root (if not exists)
2. Create `data/content.json` with the following structure:

**File content (exact JSON structure per D-02):**

```json
{
  "metadata": {
    "gym_name": "GripGym",
    "version": "1.0",
    "last_updated": "2026-07-21",
    "address": "Old Market Road, Bhadrachalam, Telangana 507111"
  },
  "pricing": {
    "intro": "Flexible membership plans for every fitness level",
    "tiers": [
      {
        "id": "basic",
        "name": "Basic Membership",
        "price": 999,
        "duration": "per month",
        "features": [
          "Gym access during off-peak hours (9 AM - 5 PM)",
          "Full equipment access",
          "1 welcome session with trainer",
          "Locker facility"
        ]
      },
      {
        "id": "premium",
        "name": "Premium Membership",
        "price": 1999,
        "duration": "per month",
        "features": [
          "24/7 gym access",
          "All classes included (unlimited)",
          "2 personal training sessions/month",
          "Nutrition consultation",
          "Locker + towel service",
          "Priority class booking"
        ]
      },
      {
        "id": "elite",
        "name": "Elite Pro Membership",
        "price": 3499,
        "duration": "per month",
        "features": [
          "24/7 gym access",
          "Unlimited all classes",
          "8 personal training sessions/month",
          "Monthly nutrition planning",
          "Locker + premium amenities",
          "Guest privileges (2x per week)",
          "Recovery room access",
          "Priority in all programs"
        ]
      },
      {
        "id": "annual",
        "name": "Annual Elite",
        "price": 35999,
        "duration": "per year",
        "discount_label": "Save ₹6,000/year",
        "features": [
          "24/7 gym access",
          "Unlimited all classes",
          "10 personal training sessions/month",
          "Monthly nutrition planning",
          "Premium locker + amenities",
          "Guest privileges (3x per week)",
          "Recovery room access",
          "Priority in all programs",
          "Quarterly fitness assessments"
        ]
      }
    ]
  },
  "contact": {
    "phone": "+91-9876543210",
    "email": "info@gripgym.com",
    "address": "Old Market Road, Bhadrachalam, Telangana 507111",
    "hours": {
      "weekdays": "6:00 AM - 10:00 PM",
      "weekends": "7:00 AM - 9:00 PM"
    },
    "form_type": "email",
    "social_media": {
      "instagram": "https://instagram.com/gripgym",
      "facebook": "https://facebook.com/gripgym"
    }
  }
}
```

**Validation:**
- Ensure valid JSON: No trailing commas, all quotes are double quotes, all braces/brackets matched
- Verify all 4 pricing tiers present with complete feature lists (no Lorem Ipsum)
- Verify contact info complete (phone, email, address, hours)
- Verify metadata section with gym name and version

**Note (D-01, D-02):** This JSON is the single source of truth for pricing and contact content. No hardcoded values should appear in HTML. Wave 2 will fetch and render from this file.
  </action>
  <verify>
    <automated>
Verify file exists and valid JSON syntax:
- `test -f data/content.json && echo "✓ File exists"` (PowerShell: `Test-Path data/content.json`)
- `node -e "const c = require('./data/content.json'); console.log('✓ Valid JSON, pricing tiers:', c.pricing.tiers.length)"` 
- Verify 4 tiers: `node -e "const c = require('./data/content.json'); console.log('Tiers:', c.pricing.tiers.map(t => t.id).join(', '))"`
- Verify contact fields: `node -e "const c = require('./data/content.json'); console.log('Contact:', c.contact.phone, c.contact.email)"`
    </automated>
  </verify>
  <done>
✅ `data/content.json` exists with valid JSON syntax
✅ All 4 pricing tiers present (basic, premium, elite, annual) with complete feature lists
✅ Contact info complete (phone, email, address, hours)
✅ Metadata section includes gym name, version, last_updated, address
✅ No hardcoded values in HTML/CSS (all data is in JSON)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON file → Browser | Static JSON file loaded via fetch; untrusted but parsed safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | JSON file (data/content.json) | accept | JSON file is source-controlled; modifications tracked by git |
| T-05-02 | Information Disclosure | Pricing data | accept | Pricing is intended to be public; no sensitive data in JSON |
| T-05-03 | Denial of Service | JSON parse failure | mitigate | Wave 2 will add try/catch around JSON.parse() with error logging |

</threat_model>

<verification>
1. JSON file created and valid
2. All pricing tiers with correct prices and features
3. Contact information complete
4. File is ready for Wave 2 fetch operations
</verification>

<success_criteria>
- [ ] `data/content.json` exists and is valid JSON
- [ ] 4 pricing tiers with real prices (999, 1999, 3499, 35999)
- [ ] Contact info includes phone, email, address, hours
- [ ] No syntax errors in JSON file
</success_criteria>

<output>
Create `.planning/phases/05-pricing-contact/05-01-SUMMARY.md` when complete
</output>
